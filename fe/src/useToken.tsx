// TODO: implement this hook to fetch the token from the endpoint
// it should cache the result, so only make a single request
export function useToken(): [number, boolean] {
  // Replace this with your own implementation
  const loading = false
  return [1, loading]
}